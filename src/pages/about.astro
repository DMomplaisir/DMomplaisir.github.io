---
/**
 * About Page - Dylan Momplaisir
 * Product thinking + storytelling focus
 * Architectural aesthetic with warm minimalism
 */

import Footer from '../components/Footer.astro';

// Import design system styles
import '../styles/variables.css';
import '../styles/fonts.css';

const pageTitle = 'About - Dylan Momplaisir';
const pageDescription = 'From urbanism to full-stack engineering—building digital infrastructure that transforms how we tell stories and understand our world.';
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content={pageDescription}>
  <title>{pageTitle}</title>

  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">

  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
</head>

<body>
  <!-- Subtle Grid Background -->
  <div class="grid-background" aria-hidden="true"></div>

  <!-- Header -->
  <header class="page-header">
    <nav class="nav-breadcrumb">
      <a href="/" class="breadcrumb-link">Home</a>
      <span class="breadcrumb-separator">/</span>
      <span class="breadcrumb-current">About</span>
    </nav>
  </header>

  <!-- Main Content -->
  <main class="about-content">
    <div class="container-narrow">
      <!-- Hero Statement -->
      <section class="hero-statement">
        <h1 class="page-title">Building bridges between bits and narratives</h1>
        <p class="lead-text">
          I'm a full-stack engineer who thinks like a product builder and
          approaches problems like an urbanist—focused on systems, patterns,
          and how people actually experience the things we create.
        </p>
      </section>

      <!-- Core Philosophy -->
      <section class="philosophy-section">
        <h2 class="section-heading">How I think about building</h2>
        <div class="philosophy-grid">
          <div class="philosophy-card">
            <h3 class="card-heading">Systems over features</h3>
            <p class="card-text">
              Good products aren't collections of features—they're systems that
              solve real problems. I focus on understanding the full context:
              user needs, technical constraints, and organizational goals.
            </p>
          </div>

          <div class="philosophy-card">
            <h3 class="card-heading">Impact over activity</h3>
            <p class="card-text">
              Engineering is about outcomes, not output. I measure success by
              what changed for users and the business—whether that's 2x
              engagement, 733% growth, or enabling new editorial possibilities.
            </p>
          </div>

          <div class="philosophy-card">
            <h3 class="card-heading">Craft over perfection</h3>
            <p class="card-text">
              Great products ship. I believe in building with care, iterating
              based on reality, and knowing when good enough beats perfect.
              Speed and quality aren't opposites—they're both essential.
            </p>
          </div>

          <div class="philosophy-card">
            <h3 class="card-heading">Cross-functional fluency</h3>
            <p class="card-text">
              I invest in understanding how different functions work—engineering,
              design, editorial, product—so I can communicate more effectively
              and collaborate more smoothly. Better understanding leads to better work.
            </p>
          </div>
        </div>
      </section>

      <!-- Journey -->
      <section class="journey-section">
        <h2 class="section-heading">The path from cities to code</h2>

        <div class="timeline">
          <div class="timeline-item">
            <div class="timeline-marker"></div>
            <div class="timeline-content">
              <h3 class="timeline-heading">Urbanism & Systems Thinking</h3>
              <p class="timeline-text">
                Growing up in Queens, New York within a vibrant Caribbean community
                shaped how I see neighborhoods and cities—as living systems where
                small changes ripple outward. I studied urban planning and design,
                eventually writing my thesis on neighborhood change in Bushwick and
                South Queens. Cities are just platforms—like software, but slower.
              </p>
            </div>
          </div>

          <div class="timeline-item">
            <div class="timeline-marker"></div>
            <div class="timeline-content">
              <h3 class="timeline-heading">Journalism & Storytelling</h3>
              <p class="timeline-text">
                Realized that telling stories about the world required building
                the infrastructure to support those stories. At The Amherst
                Student, grew readership 733% by modernizing how journalism
                worked digitally.
              </p>
            </div>
          </div>

          <div class="timeline-item">
            <div class="timeline-marker"></div>
            <div class="timeline-content">
              <h3 class="timeline-heading">Engineering & Product</h3>
              <p class="timeline-text">
                Now at The Atlantic, building platforms that serve millions.
                The urbanism background shows up in how I think about
                architecture, user flows, and sustainable systems. Engineering
                is the tool, but product thinking is the foundation.
              </p>
            </div>
          </div>
        </div>
      </section>

      <!-- Current Focus -->
      <section class="current-focus">
        <h2 class="section-heading">What I'm focused on now</h2>
        <div class="focus-content">
          <p class="body-text">
            Currently building audio infrastructure at The Atlantic, working on
            problems of scale, reliability, and editorial velocity. Increasingly
            interested in product leadership and the intersection of AI/ML with
            the systems I build.
          </p>
          <p class="body-text">
            Outside of work, I'm thinking about:
          </p>
          <ul class="focus-list">
            <li>Growing AI/ML capabilities and how they change product development</li>
            <li>Moving toward product leadership roles where I can shape direction, not just execution</li>
            <li>Building sustainable systems that outlast their creators</li>
            <li>The role of technology in civic infrastructure and journalism</li>
          </ul>
        </div>
      </section>

      <!-- Tech & Skills (Lightweight) -->
      <section class="skills-section">
        <h2 class="section-heading">How I build</h2>
        <div class="skills-grid">
          <div class="skill-category">
            <h3 class="skill-heading">Product Engineering</h3>
            <p class="skill-text">Next.js, React, TypeScript, GraphQL, Node.js, Django, Python</p>
          </div>
          <div class="skill-category">
            <h3 class="skill-heading">Infrastructure & Scale</h3>
            <p class="skill-text">AWS, GCP, PostgreSQL, Redis, monitoring, performance optimization</p>
          </div>
          <div class="skill-category">
            <h3 class="skill-heading">Product & Leadership</h3>
            <p class="skill-text">Cross-functional collaboration, technical architecture, team leadership</p>
          </div>
          <div class="skill-category">
            <h3 class="skill-heading">Tools & Process</h3>
            <p class="skill-text">Linear, Figma, Git, CI/CD, agile workflows</p>
          </div>
        </div>
      </section>

      <!-- CTA Section -->
      <section class="cta-section">
        <h2 class="section-heading">Let's work together</h2>
        <p class="body-text">
          I'm interested in opportunities where I can bring product thinking,
          full-stack engineering, and systems design to meaningful problems—particularly
          in journalism, civic tech, or products that help people understand
          complex systems.
        </p>
        <div class="cta-buttons">
          <a href="/contact" class="btn-primary">Get in touch</a>
          <a href="/resume.pdf" class="btn-secondary" download>Download Resume</a>
        </div>
      </section>
    </div>
  </main>

  <!-- Footer -->
  <Footer />
</body>
</html>

<style>
  /* ==================== GLOBAL RESET ==================== */

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    font-family: var(--font-body);
    color: var(--text-primary);
    background-color: var(--warm-white);
    line-height: var(--leading-normal);
    overflow-x: hidden;
    position: relative;
  }

  /* ==================== GRID BACKGROUND ==================== */

  .grid-background {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image:
      linear-gradient(var(--blueprint-line) 1px, transparent 1px),
      linear-gradient(90deg, var(--blueprint-line) 1px, transparent 1px);
    background-size: var(--grid-unit) var(--grid-unit);
    opacity: 0.5;
    pointer-events: none;
    z-index: 0;
  }

  /* ==================== HEADER ==================== */

  .page-header {
    position: relative;
    padding: var(--space-xxl) var(--space-lg) var(--space-lg);
    max-width: var(--width-content);
    margin: 0 auto;
    z-index: 1;
  }

  .nav-breadcrumb {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--concrete-400);
    letter-spacing: var(--tracking-wide);
  }

  .breadcrumb-link {
    color: var(--burgundy-primary);
    text-decoration: none;
    transition: color var(--transition-fast);
  }

  .breadcrumb-link:hover {
    color: var(--burgundy-hover);
  }

  .breadcrumb-separator {
    margin: 0 var(--space-xs);
    color: var(--concrete-300);
  }

  .breadcrumb-current {
    color: var(--concrete-900);
  }

  /* ==================== MAIN CONTENT ==================== */

  .about-content {
    position: relative;
    padding: 0 var(--space-lg) var(--space-xxxl);
    z-index: 1;
  }

  .container-narrow {
    max-width: var(--width-narrow);
    margin: 0 auto;
  }

  /* ==================== HERO STATEMENT ==================== */

  .hero-statement {
    margin-bottom: var(--space-xxxl);
    padding-bottom: var(--space-xxxl);
    border-bottom: var(--border-thin) solid var(--warm-gray-200);
  }

  .page-title {
    font-family: var(--font-display);
    font-size: clamp(2.5rem, 6vw, 4rem);
    font-weight: var(--weight-regular);
    color: var(--concrete-900);
    letter-spacing: var(--tracking-tight);
    line-height: var(--leading-tight);
    margin-bottom: var(--space-xl);
  }

  .lead-text {
    font-size: var(--text-xl);
    line-height: var(--leading-relaxed);
    color: var(--concrete-400);
  }

  /* ==================== SECTIONS ==================== */

  .philosophy-section,
  .journey-section,
  .current-focus,
  .skills-section,
  .cta-section {
    margin-bottom: var(--space-xxxl);
  }

  .section-heading {
    font-family: var(--font-display);
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    font-weight: var(--weight-regular);
    color: var(--concrete-900);
    margin-bottom: var(--space-xl);
    letter-spacing: var(--tracking-tight);
  }

  /* ==================== PHILOSOPHY GRID ==================== */

  .philosophy-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--space-lg);
  }

  .philosophy-card {
    background: var(--cream);
    border: var(--border-thin) solid var(--warm-gray-200);
    border-radius: var(--radius-base);
    padding: var(--space-lg);
    transition: all var(--transition-base);
  }

  .philosophy-card:hover {
    border-color: var(--burgundy-primary);
    transform: translateY(-2px);
    box-shadow: var(--shadow-sm);
  }

  .card-heading {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: var(--weight-regular);
    color: var(--concrete-900);
    margin-bottom: var(--space-sm);
  }

  .card-text {
    font-size: var(--text-base);
    line-height: var(--leading-relaxed);
    color: var(--concrete-400);
  }

  /* ==================== TIMELINE ==================== */

  .timeline {
    position: relative;
    padding-left: var(--space-xl);
  }

  .timeline::before {
    content: '';
    position: absolute;
    left: 8px;
    top: 0;
    bottom: 0;
    width: 2px;
    background: var(--warm-gray-200);
  }

  .timeline-item {
    position: relative;
    margin-bottom: var(--space-xxl);
  }

  .timeline-item:last-child {
    margin-bottom: 0;
  }

  .timeline-marker {
    position: absolute;
    left: calc(-1 * var(--space-xl));
    top: 4px;
    width: 20px;
    height: 20px;
    border-radius: 50%;
    background: var(--burgundy-primary);
    border: 3px solid var(--warm-white);
    box-shadow: 0 0 0 2px var(--warm-gray-200);
  }

  .timeline-heading {
    font-family: var(--font-display);
    font-size: var(--text-lg);
    font-weight: var(--weight-medium);
    color: var(--concrete-900);
    margin-bottom: var(--space-sm);
  }

  .timeline-text {
    font-size: var(--text-base);
    line-height: var(--leading-relaxed);
    color: var(--concrete-400);
  }

  /* ==================== CURRENT FOCUS ==================== */

  .focus-content {
    max-width: 65ch;
  }

  .body-text {
    font-size: var(--text-base);
    line-height: var(--leading-relaxed);
    color: var(--concrete-400);
    margin-bottom: var(--space-md);
  }

  .focus-list {
    list-style: none;
    margin-top: var(--space-md);
  }

  .focus-list li {
    position: relative;
    padding-left: var(--space-lg);
    font-size: var(--text-base);
    line-height: var(--leading-relaxed);
    color: var(--concrete-400);
    margin-bottom: var(--space-sm);
  }

  .focus-list li::before {
    content: '→';
    position: absolute;
    left: 0;
    color: var(--burgundy-primary);
    font-weight: var(--weight-medium);
  }

  /* ==================== SKILLS GRID ==================== */

  .skills-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--space-lg);
  }

  .skill-category {
    padding: var(--space-lg);
    background: var(--cream);
    border-left: 3px solid var(--burgundy-primary);
  }

  .skill-heading {
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    font-weight: var(--weight-medium);
    color: var(--burgundy-primary);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wider);
    margin-bottom: var(--space-xs);
  }

  .skill-text {
    font-size: var(--text-base);
    line-height: var(--leading-relaxed);
    color: var(--concrete-400);
  }

  /* ==================== CTA SECTION ==================== */

  .cta-section {
    text-align: center;
    padding: var(--space-xxl);
    background: var(--cream);
    border-radius: var(--radius-lg);
    border: var(--border-thin) solid var(--warm-gray-200);
  }

  .cta-section .section-heading {
    margin-bottom: var(--space-md);
  }

  .cta-section .body-text {
    max-width: 60ch;
    margin-left: auto;
    margin-right: auto;
    margin-bottom: var(--space-xl);
  }

  .cta-buttons {
    display: flex;
    gap: var(--space-sm);
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn-primary,
  .btn-secondary {
    padding: calc(var(--space-sm) - 2px) var(--space-lg);
    text-decoration: none;
    border-radius: var(--radius-base);
    font-weight: var(--weight-medium);
    font-size: var(--text-base);
    transition: all var(--transition-base);
    display: inline-block;
    border: var(--border-thin) solid transparent;
  }

  .btn-primary {
    background: var(--burgundy-primary);
    color: var(--warm-white);
    border-color: var(--burgundy-primary);
  }

  .btn-primary:hover {
    background: var(--burgundy-hover);
    border-color: var(--burgundy-hover);
    transform: translateY(-2px);
    box-shadow: var(--shadow-burgundy);
  }

  .btn-secondary {
    background: transparent;
    color: var(--concrete-900);
    border-color: var(--concrete-300);
  }

  .btn-secondary:hover {
    border-color: var(--burgundy-primary);
    color: var(--burgundy-primary);
    transform: translateY(-2px);
  }

  /* ==================== RESPONSIVE ==================== */

  @media (max-width: 768px) {
    .page-header {
      padding: var(--space-xl) var(--space-md) var(--space-md);
    }

    .about-content {
      padding: 0 var(--space-md) var(--space-xxl);
    }

    .philosophy-grid {
      grid-template-columns: 1fr;
    }

    .skills-grid {
      grid-template-columns: 1fr;
    }

    .timeline {
      padding-left: var(--space-lg);
    }

    .cta-buttons {
      flex-direction: column;
      align-items: center;
    }

    .btn-primary,
    .btn-secondary {
      width: 100%;
      max-width: 280px;
      text-align: center;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    * {
      animation-duration: 0.01ms !important;
      transition-duration: 0.01ms !important;
    }
  }
</style>
